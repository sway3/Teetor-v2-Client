import { useAuthContext } from '@/provider/AuthContext';
import { useContext } from 'react';

export default function DashboardPage() {
  return <h1 className="h-[100vh] text-2xl font-bold">Dashboard</h1>;
}
